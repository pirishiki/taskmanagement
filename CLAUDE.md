# taskmanagement 開発ルール

このリポジトリで作業するときは、以下のルールを**必ず**守ること。例外はない。

## Git / GitHub の運用ルール

作業は必ず「イシュー → ブランチ → PR → master にマージ」の順で進める。

1. **作業を始める前に最新化する**
   `git switch master` → `git pull` を実行してから始める。

2. **イシューを必ず作る**
   - コードやファイルを変更する前に、`gh issue create` でイシューを作る。
   - 既存のイシューで対応する場合は、その番号をユーザーに確認する。
   - イシューのない作業はしない。

3. **ブランチを必ず作る**
   - 名前は `<種類>/<イシュー番号>-<短い英語の説明>` にする（例：`feature/5-task-list`）。
   - 種類は次のどれか。
     | 種類 | 使うとき |
     |---|---|
     | `feature` | 新しい機能を追加する |
     | `fix` | 正しく動かないものを直す |
     | `docs` | ドキュメントだけを変更する |
     | `refactor` | 動きを変えずにコードを整理する |
     | `chore` | 設定・ツール・依存関係など、アプリの動きが変わらない作業 |

4. **master には直接触らない**
   - master への直接コミット、直接プッシュ、force push は禁止。
   - GitHub のルールセット `protect-master` でも拒否される。
   - `.claude/hooks/guard-master.ps1` のフックでも止められる。

5. **PR を作る**
   - `gh pr create` で PR を作る。本文には `Closes #<イシュー番号>` を書く。こう書くと、マージしたときにイシューが自動で閉じる。
   - **マージはユーザーの確認を取ってから行う。** 勝手にマージしない。
   - マージ後は `git switch master` → `git pull` で最新化する。

## 教え方のルール

ユーザーはプログラミング初心者。

- 小さく段階的に進める。1回で進むのはファイル1つ、または考え方1つまで。
- 新しい言葉は、使う前に身近なたとえを交えて説明する。
- 各ステップの終わりに理解度チェックの質問をする。ただし、説明していない言葉を問題にしない。
- 次へ進むかどうかはユーザーに選んでもらう。
