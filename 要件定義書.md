# タスク管理アプリ 要件定義書

## 1. 概要

Trelloのような、カード形式でタスクを管理するWebアプリを作成する。
本ドキュメントは学習目的で作成するアプリの要件を整理したものである。

初期版はHTML/CSS/素のJavaScriptのみで作成したプロトタイプ（[index.html](index.html) / [script.js](script.js) / [style.css](style.css)、データはブラウザのlocalStorageに保存）であった。
今回、技術スタックの指定によりフロントエンドをReact、バックエンドをJava（Spring Boot）+ PostgreSQLデータベースの構成に変更する（詳細は4節）。プロトタイプ版は移行前の参考実装として残す。

## 2. 目的

- カンバン方式（列にカードを並べる方式）でタスクを視覚的に管理できるようにする
- React（フロントエンド）とJava/Spring Boot（バックエンド）を用いたクライアント・サーバー構成のアプリ開発の基礎を学ぶ

## 3. 対象ユーザー

- 開発者本人（個人利用・学習目的）

## 4. 利用環境・システム構成

### 4.1 技術スタック

#### フロントエンド

| 区分 | 技術 | バージョン |
| --- | --- | --- |
| 実行環境 | Node.js | 24.19.0（LTS） |
| フレームワーク | React（Next.js等のフレームワークは使用しない） | 19.3.0 |
| 言語 | TypeScript | 6.0.3 |
| ビルドツール | Vite | 8.3.0 |
| パッケージ管理 | npm | 11.17.0 |
| ドラッグ＆ドロップ | dnd-kit（@dnd-kit/core・@dnd-kit/sortable） | 6.3.1・10.0.0 |
| スタイリング | Tailwind CSS | 4.3.3 |
| 静的解析 | oxlint | 1.81 |

#### バックエンド

| 区分 | 技術 | バージョン |
| --- | --- | --- |
| 言語 | Java | 21（Eclipse Temurin 21.0.12.1、LTS） |
| フレームワーク | Spring Boot | 4.1.1 |
| ビルドツール | Gradle | 9.7.1（`gradlew`ラッパー経由） |
| API形式 | REST API | - |

主要な部品（ライブラリ）。いずれも `build.gradle` で直接指定しているものではなく、Spring Boot 4.1.1 が組み合わせを決めて自動で取り寄せているもの。

| 区分 | 技術 | バージョン |
| --- | --- | --- |
| Web（REST API） | Spring Web MVC | 7.0.9 |
| Webサーバー（組み込み） | Apache Tomcat | 11.0.24 |
| JSON変換 | Jackson | 3.1.5 |
| DBアクセス | Spring Data JPA | 4.1.1 |
| DBアクセス（ORM） | Hibernate ORM | 7.4.5 |
| DB接続プール | HikariCP | 7.0.2 |
| DBドライバ | PostgreSQL JDBC Driver | 42.7.13 |
| 入力チェック | Hibernate Validator | 9.1.3 |

#### データベース

| 区分 | 技術 | バージョン |
| --- | --- | --- |
| RDBMS | PostgreSQL（Dockerコンテナ） | 17.11 |

#### 開発ツール

| 区分 | 技術 | バージョン |
| --- | --- | --- |
| バージョン管理 | Git | 2.55.0 |
| リポジトリホスティング | GitHub | - |

※ フロントエンドのバージョンは `frontend/package-lock.json` に実際にインストールされたもの。

### 4.2 全体構成

- ブラウザで動作するReact製フロントエンドが、Spring Bootで実装したREST APIにHTTP経由でアクセスする
- バックエンドはPostgreSQLデータベースに対してタスクデータの読み書きを行う
- 構成イメージ: `ブラウザ（React）→ HTTP(REST API) → Spring Boot → PostgreSQL`
- 動作環境: Webブラウザ（Google Chrome等）＋ バックエンドサーバー（Javaが動作する環境）＋ PostgreSQLサーバー

## 5. 機能要件

### 5.1 v1のスコープ（実装済み＋実装対象）

以下はv1として取り組む機能。

※ 「状態」欄はプロトタイプ版（HTML/CSS/JavaScript + localStorage）における実装状況を示す。React + Spring Boot + PostgreSQLの新構成では、これらの機能はいずれも未着手であり、今後1つずつ移植・再実装する。

| No  | 機能    | 内容                            | 状態 |
| --- | ----- | ----------------------------- | --- |
| 1   | 列表示   | 「やるべきこと」「進行中」「終わったこと」の3列を表示する | 実装済み |
| 2   | タスク追加 | 各列の入力欄からタスクを追加できる。追加時に優先度（既定は「中」）と期限日（任意）も合わせて設定できる。タスク名が空欄または空白のみの場合は、追加ボタンを押しても何も起こらない（カードは作成されず、エラー表示も行わない）※Trelloと同様の挙動（※新構成でも、POST /api/tasks による登録を API・React 画面とも実装済み） | 実装済み |
| 3   | タスク削除 | カードの「×」ボタンでタスクを削除できる（※新構成でも実装済み。押し間違いを防ぐため、削除の前に「『タスク名』を削除しますか？」と確認し、OK のときだけ削除する） | 実装済み |
| 4   | タスク移動 | カードをドラッグ&ドロップして列を移動できる（※新構成でも実装済み。ドラッグ中は移動先の列のカードが場所を空け、着地点が見える。カード左の ○ を押すと、PATCH で「終わったこと」の列の一番下に移る） | 実装済み |
| 5   | データ保存 | タスク情報はブラウザに保存し、再読み込みしても消えない   | 実装済み |
| 6   | 保存データ破損時の挙動 | アプリ起動時、保存データの読み込みに失敗した場合は「保存データが壊れています。空の状態から始めますか？」と確認メッセージを表示する。ユーザーが同意した場合のみタスクが1つもない状態で起動し、同意しない場合はそれ以上進めない | 実装済み |
| 7   | タスク編集 | 追加済みタスクの文言を後から編集できる（※新構成でも、カードの ✎ から開くクイック編集で、タスク名・優先度・期限日をまとめて PUT で更新できる。タスク名が空欄なら元のまま） | 実装済み |
| 8   | 期限設定 | タスクの追加時、および追加後にいつでも締切日を設定・変更できる（※新構成でも、追加フォームとクイック編集で設定・変更・削除できる） | 実装済み |
| 9   | 優先度設定 | タスクの追加時、および追加後にいつでも「高・中・低」の優先度を設定・変更できる（※新構成でも、追加フォームとクイック編集で設定・変更できる） | 実装済み |
| 10  | 列内の手動並び替え | 同一列内でカードをドラッグして上下の順序を入れ替えられる（※新構成でも実装済み。ただし検索で絞り込んでいる間は、並び順がずれるのを防ぐため、ドラッグできない。No.16 を参照） | 実装済み |
| 11  | 列内の自動並び替え | 列ごとに「優先度順」「期限が近い順」を選ぶと、その列のカードが一括で並び替わる。選択後は再び手動でドラッグして順序を調整できる（※新構成でも実装済み。同じ優先度・同じ期限のカードどうしは、今の順番のまま残る。検索で絞り込んでいる間は、No.10 と同じ理由で使えない） | 実装済み |

### 5.2 未実装・今後の拡張候補（v2以降）


| No  | 機能       | 内容                            | 優先度        |
| --- | -------- | ----------------------------- | ---------- |
| 12  | 検索・絞り込み  | タスク名で検索、優先度や期限で絞り込みできる（※タスク名のキーワード検索は API・React 画面とも実装済み。優先度・期限での絞り込みは未実装） | 低          |
| 13  | 列の追加・削除  | 「To Do / 進行中 / 完了」以外の列を自由に作れる | 低          |
| 14  | 複数デバイス同期 | サーバー・データベースを用意し、他端末とも同期する     | 低（要サーバー実装） |
| 15  | データのエクスポート/インポート・バックアップ | タスクデータをファイルに書き出し・読み込みできるようにする。保存データが壊れた場合の復旧手段としても検討する | 低 |
| 16  | 検索中の並び替え | 検索で絞り込んでいるときも、ドラッグ&ドロップで並び替えられるようにする。並び順を「前後のカードの間の番号（小数）」で持ち、動かしたカードの番号だけを変える方式にする必要がある（sortOrder の型の変更、並び替え API の作り直しを伴う） | 低 |
| 17  | 自動テスト | 削除や並び順の決め方（新しいカードは列の最大値＋1 の番号で一番下に入る）など、主な処理が正しく動くかを自動で確かめるテストを書く。コードを変えたときに、前に直した不具合が戻ってくる（デグレ）のを早く見つけるため。今あるバックエンドのテストは、アプリが起動できるかの確認（BackendApplicationTests）だけ。また、今のテストは起動中の Docker の DB に依存しているため、テストのたびに使い捨ての DB を用意する Testcontainers を入れ、GitHub Actions でもテストを動かせるようにする（2026-09-27 の品質チェック #11） | 低 |
| 18  | API の返事専用の型（DTO） | DB の形（エンティティ `Task`）をそのまま API の返事にせず、返事専用の型を作って返す。DB の形を変えても画面側が壊れないようにするため（品質チェック #5）（※実装済み：`TaskResponse`。イシュー #21） | 実装済み |
| 19  | status・priority の enum 化 | status と priority を文字列＋正規表現ではなく、Java の enum（決まった値だけを持てる型）で持つ。決まった値以外をそもそも作れないようにするため（品質チェック #6）（※実装済み：`TaskStatus`・`TaskPriority`。JSON と DB では今までどおり小文字。イシュー #21） | 実装済み |
| 20  | テーブル変更の記録（Flyway） | 起動のたびに Hibernate がテーブルを自動で直す（`ddl-auto=update`）のをやめ、テーブルの変更を SQL ファイルとして順番に記録する。いつ何を変えたかを残し、本番でも同じ変更を安全に行えるようにするため（品質チェック #9） | 低 |
| 21  | エラーの返事の形の統一（グローバル例外ハンドラー・ProblemDetail） | グローバル例外ハンドラー（`@RestControllerAdvice` を付けたクラス）を作り、アプリのどこで起きたエラーも1か所で受け止めて、Spring 標準の ProblemDetail の形で返す。あわせて画面のエラーメッセージを、サーバーの返事（400・404・500 など）ごとに変える。この3つは1つの PR で入れる（品質チェック #10・#16） | 低 |
| 22  | 編集画面の表示の仕組み（createPortal） | クイック編集の画面を、開いた時点のカードの位置に固定するのではなく、React の createPortal で画面の一番外側に表示する。スクロールや画面サイズの変更で位置がずれないようにするため（品質チェック #15） | 低 |
| 23  | 書き方の決まりのチェック（Checkstyle） | バックエンドに Checkstyle を入れ、字下げ・1行の長さ・名前の付け方・使っていない import などの「書き方の決まり」を自動でチェックする。PMD（バグになりやすい書き方を見る）では見つけられない種類の問題で、品質チェックの #19（タブとスペースの混在）の再発を防ぐため。No.17（自動テスト・Testcontainers）と同じ PR で、GitHub Actions のチェックに加える | 低 |


※ v2以降の機能は本アプリの現段階では未着手。実装する場合は本書を更新すること。

## 6. 画面構成



### 6.1 このアプリの考え方（カンバン方式とは）

「カンバン方式」とは、タスクを1枚ずつカードにして、進み具合ごとに列（レーン）を分けて並べる管理方法である。
カードを別の列にドラッグ＆ドロップで移動させるだけで、タスクの状態（未着手→作業中→完了）を更新できる。
紙の付箋をホワイトボードに貼って進捗を動かしていくイメージに近い。有名なツールに Trello, Jira 等があるが、
本アプリはその基本機能のみを自作するものである。

### 6.2 画面イメージ

![画面イメージ](docs/screen-mockup.svg)

- 1画面構成（シングルページ）。ログインなどはなく、開くと即座にこの画面が表示される
- 画面内に3列（**やるべきこと** / **進行中＝作業中** / **終わったこと**）を横並びで表示
- 1つのタスク＝1枚の「カード」。カードは白背景の四角で表示され、右上に編集アイコン（✎）と削除の「×」を配置する
  - ✎ と × は、マウスを乗せたときだけ出す（ホバー表示）のではなく、いつも表示する。ホバー表示は画面がすっきりする一方で、ボタンがあることに気づきにくく、初めて使う人が「削除はどこ？」と迷いやすいため。見つけやすさを優先する
  - ✎ と × が隣り合うことによる押し間違いは、削除の前に確認のダイアログを出すことで防ぐ（No.3）
- カード下部に優先度（高・中・低、色つきの丸印で表示）と期限日を表示する
- カードをつかんで別の列にドラッグすると、タスクの状態（未着手→進行中→完了）が切り替わる
- 各列の下部に「タスク名入力欄＋追加ボタン」を配置し、その列に直接新しいタスクを追加できる
- 各列の見出し部分に並び替え用のセレクトボックス（「並び替え」「優先度順」「期限が近い順」）を配置する
- 同一列内でカードをドラッグして別のカードの上/下にドロップすると、その位置に順序が入れ替わる（列をまたぐ移動は既存の「タスク移動」と同様の挙動）
- 列の並び替えセレクトで「優先度順」または「期限が近い順」を選択すると、その列内のカードが選択した基準で一括で並び替わる。並び替えは選択した時点で一度だけ実行され、以降はまた自由に手動ドラッグで順序を調整できる（常時そのソート順が維持され続けるわけではない）
  - 「期限が近い順」では、期限日が未設定のカードは末尾に配置する
- 列内にタスクが1件もない場合、「タスクがありません」等の案内文は表示せず、単に空の状態のまま表示する（Trelloと同様の挙動）



### 6.3 補足

- 上図はワイヤーフレーム（構成を示す簡易図）であり、実際の配色・フォントは実装（[style.css](style.css)）に準ずる
- 実物は [index.html](index.html) をブラウザで開くことで確認できる（いずれもプロトタイプ版。React版のUIは本レイアウト・挙動を踏襲して作成する）



## 7. データ構造

### 7.1 新構成（React + Spring Boot + PostgreSQL）

タスク1件をPostgreSQLデータベースの`tasks`テーブルに以下の形式で保存する（Spring Data JPAのエンティティに対応）。

| カラム | 型 | 説明 |
| --- | --- | --- |
| id | Long（自動採番） | タスクの一意なID |
| text | String | タスクの内容 |
| status | String | `todo` \| `doing` \| `done` のいずれか |
| priority | String | `high` \| `medium` \| `low` のいずれか（新規追加時は`medium`） |
| dueDate | LocalDate（nullable） | 期限日。未設定の場合はnull |
| sortOrder | Integer | 同じ列（status）内での表示順。値が小さいほど上に表示される |

列内の表示順は`sortOrder`で管理する（配列の並び順ではなくDBカラムとして明示的に持つ）。手動ドラッグや自動並び替えは、対象タスクの`sortOrder`（および列をまたぐ場合は`status`）を更新することで実現する。

フロントエンド（React）は以下のREST APIを介してこのデータを読み書きする。

| メソッド | パス | 内容 |
| --- | --- | --- |
| GET | /api/tasks | 全タスクを取得する（status→sortOrderの順） |
| GET | /api/tasks?keyword={キーワード} | タスク名にキーワードを含むタスクを取得する（部分一致・大文字小文字を区別しない） |
| GET | /api/tasks/{id} | タスクを1件取得する（存在しない場合は404） |
| POST | /api/tasks | タスクを新規作成する（列の一番下に追加。status 省略時は todo、priority 省略時は medium。成功時は201、text が空欄・空白のみ、または status／priority が決められた値以外なら400） |
| PUT | /api/tasks/{id} | タスクを丸ごと更新する（text/priority/dueDate/status。status／priority 省略時は今の値のまま、dueDate 省略時は期限なしになる。列が変わるときは移動先の一番下に置く。成功時は200、存在しない場合は404、入力が不正なら400） |
| PATCH | /api/tasks/{id} | タスクの一部だけを更新する（送った項目だけを書き換える。完了ボタンでは status だけを送る。dueDate を消すことはできないので、PUT を使う。成功時は200、存在しない場合は404、入力が不正なら400） |
| DELETE | /api/tasks/{id} | タスクを削除する |
| PUT | /api/tasks/reorder | 指定した列（status）内のタスクIDの並び順をまとめて更新する（ドラッグ並び替え・自動並び替えの両方で使用） |

### 7.2 プロトタイプ版（参考・localStorage）

初期プロトタイプでは、タスク1件を以下の形式でJavaScriptオブジェクトとして管理し、localStorageにJSON形式で保存していた。並び順は配列内の並び順そのもので表現していた（順序専用フィールドなし）。

```json
{
  "id": "タスクごとの一意なID（文字列）。crypto.randomUUID() を用いて生成する",
  "text": "タスクの内容（文字列）",
  "status": "todo | doing | done のいずれか",
  "priority": "high | medium | low のいずれか（新規追加時は medium）",
  "dueDate": "期限日（YYYY-MM-DD形式の文字列）。未設定の場合は空文字"
}
```



## 8. 非機能要件

- バックエンド（Spring Boot）を起動した状態で、フロントエンド（React）からブラウザ経由でアクセスして動作すること
- ブラウザを閉じてもデータが消えないこと（PostgreSQLデータベースに永続化）
- 対応ブラウザ: 最新版のGoogle Chrome / Microsoft Edge
- タスク名にHTMLタグやスクリプトのような特殊な文字列が入力された場合でも、HTML/プログラムとして解釈せず、常に単なる文字列として画面に表示すること（Reactの標準のテキスト描画はデフォルトでエスケープされるため、`dangerouslySetInnerHTML`は使用しない）
- 画面幅が狭い場合、3列の横並び表示は維持したまま、はみ出した分は横スクロールで閲覧できるようにする（`overflow-x: auto`等）。列を縦積みに組み替える等の複雑なレイアウト変更は行わない



## 9. 制約事項

- バックエンド（Spring Boot）を起動していないと、フロントエンドはデータの取得・保存ができない
- 複数人・複数端末での同時利用や共有は本書時点では想定しない（認証・権限管理の仕組みは持たない）
- 動作にはPostgreSQLサーバーが別途起動している必要がある（アプリ組み込みのDBではないため、事前にインストール・起動・データベース作成が必要）
- カードの移動はマウス操作（ドラッグ&ドロップ）のみをサポートする。キーボード操作のみでのカード移動には対応しない
- スマートフォン等のタッチ操作には対応しない。スマホでは画面の閲覧のみ可能とし、タスクの追加・削除・移動等の操作はPC（マウス操作）で行うことを前提とする
- タッチ操作の画面（スマホ等）で表示した場合は、画面上部に「スマートフォンでは閲覧のみ可能です。タスクの追加・削除・移動はPCで行ってください」という注意文を常時表示する



## 10. 受け入れ基準（動作確認チェックリスト）

v1の各機能について、以下が満たされていることを確認する。

※ 以下はプロトタイプ版（localStorage）を前提としたチェックリストである。React + Spring Boot + PostgreSQLの新構成での再実装時に、DBやAPI呼び出しを前提とした内容に見直す（例: No.9の「保存データ破損」はlocalStorage固有の想定であり、新構成ではDB接続エラー時の挙動として再定義する）。

| No | 確認項目 | 確認内容 |
| --- | --- | --- |
| 1 | 列表示 | 「やるべきこと」「進行中」「終わったこと」の3列が表示される |
| 2 | タスク追加（正常系） | 入力欄に文字を入れて追加ボタンを押すと、対象の列にカードが1枚追加される。優先度・期限を指定して追加すると、その内容がカードに反映される |
| 3 | タスク追加（空欄） | 入力欄が空欄または空白のみの状態で追加ボタンを押しても、カードは追加されない（エラー表示もない） |
| 4 | タスク削除 | カードの「×」ボタンを押し、確認で OK を押すと、そのカードが列から消える。キャンセルを押すと消えない |
| 5 | タスク移動 | カードをドラッグして別の列にドロップすると、そのカードが移動先の列に表示される |
| 6 | 列内の表示順（初期） | 特に並び替え操作をしなければ、同じ列内でカードを追加した順に上から並んで表示される |
| 7 | データ保存 | タスクを追加・削除・移動した後にブラウザをリロードしても、内容が保持されている |
| 8 | 文字列の安全表示 | タスク名にHTMLタグのような文字列（例: `<b>test</b>`）を入力しても、太字等に変換されずそのままの文字列として表示される |
| 9 | 保存データ破損時 | 保存データを意図的に壊した状態でアプリを開くと、「保存データが壊れています。空の状態から始めますか？」の確認メッセージが表示され、同意した場合のみ空の状態で起動する |
| 10 | 空の列の表示 | タスクが1件もない列を開いても、「タスクがありません」等の文言は表示されず、空のまま表示される |
| 11 | 画面幅が狭い場合の表示 | ブラウザの幅を狭くしても3列のレイアウトが崩れず、はみ出した分は横スクロールで見られる |
| 12 | スマホでの注意表示 | スマートフォン（タッチ操作の画面）で開くと、画面上部に「スマートフォンでは閲覧のみ可能です。タスクの追加・削除・移動はPCで行ってください」という注意文が表示される |
| 13 | タスク編集（正常系） | カードの編集アイコン（✎）でタスク名を変更して確定すると、カードの表示が更新され、リロード後も保持されている |
| 14 | タスク編集（空欄） | 編集中に文字を全て消して確定しても、カードは削除されず元のタスク名のまま変わらない |
| 15 | 優先度設定 | カードの優先度を高・中・低に変更すると、バッジの色とラベルが切り替わり、リロード後も保持されている |
| 16 | 期限設定 | カードに期限日を設定すると表示に反映され、リロード後も保持されている |
| 17 | 列内の手動並び替え | 同じ列内で別のカードの上/下にドラッグ&ドロップすると、その位置に順序が入れ替わり、リロード後も保持されている |
| 18 | 列内の自動並び替え（優先度順） | 列の並び替えセレクトで「優先度順」を選ぶと、その列のカードが高→中→低の順に並び替わる |
| 19 | 列内の自動並び替え（期限順） | 列の並び替えセレクトで「期限が近い順」を選ぶと、その列のカードが期限日の早い順に並び替わり、期限未設定のカードは末尾になる |

## 11. 今後の進め方

1. バックエンド（Spring Boot + PostgreSQL）のプロジェクトを作成し、7.1節のREST APIを実装する
2. フロントエンドをReactで作成し、バックエンドのAPIと接続する。プロトタイプ版（HTML/CSS/JavaScript）の機能を1つずつ移植する
3. 移植した機能ごとに10節の受け入れ基準を新構成向けに見直し、動作確認を行う
4. v2以降の機能（5.2節）から優先度の高いものを選び、1つずつ実装する
5. 機能追加のたびに本書と [README.md](README.md) を更新する

